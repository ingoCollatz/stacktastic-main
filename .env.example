# Security Environment Variables
# Add these to your .env file for enhanced security

# CSRF Protection (optional - will use CAPTCHA_SECRET as fallback)
CSRF_SECRET=your-csrf-secret-key-here

# Rate Limiting with Local Redis (optional - if not provided, rate limiting will be disabled)
REDIS_URL=redis://localhost:6379

# Alternative: Upstash Redis (cloud-based)
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Existing variables (already required)
CAPTCHA_SECRET=your-captcha-secret
MAIL_HOST=smtp.your-provider.com
MAIL_PORT=587
MAIL_SECURE=true
MAIL_USER=your-email@domain.com
MAIL_PASS=your-email-password
CONTACT_RECEIVER=contact@yourdomain.com
